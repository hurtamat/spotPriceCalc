namespace spotPriceCalc.Infrastructure.Persistence.Configurations;

// Placeholder — becomes an IEntityTypeConfiguration<BiddingZone> (Fluent mapping + one-time seed data)
// once EF Core is added. Kept in Infrastructure so the Domain entity stays persistence-ignorant.
public class BiddingZoneConfiguration
{
}
