# Direct dependencies only. Transitive ones (starlette, anyio, …) are resolved by pip;
# pinning them here would block a compatible upgrade of the packages above them.
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pandas==2.3.1
numpy==2.5.1
