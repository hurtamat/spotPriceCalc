# Copy to .env and fill in. docker-compose reads .env automatically.
ENTSOE_TOKEN=your-entsoe-security-token

# Optional: local Postgres password. Defaults to spotprice123! when unset.
# POSTGRES_PASSWORD=spotprice123!
